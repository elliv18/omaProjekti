import { makeStyles } from "@material-ui/core"

const signupStyle = makeStyles(theme => ({
    loginButton: {
        marginTop: 50
    },
    paper: {
        padding: 20
    }
}))

export default signupStyle