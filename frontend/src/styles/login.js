import { makeStyles } from "@material-ui/core";


const loginStyle = makeStyles(theme => ({
    loginButton: {
        marginTop: 50
    },
    paper: {
        padding: 20
    }
}))

export default loginStyle