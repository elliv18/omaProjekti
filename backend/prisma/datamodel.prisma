type User {
  id: ID! @id
  name: String!
}
type Vinyl {
  id: ID! @id
  name: String!
  artists: [Artist!]! @relation(name: "ArtistsVinyl")
}
type Artist {
  id: ID! @id
  name: String!
  vinyls: [Vinyl]  @relation(name: "ArtistsVinyl")
}
