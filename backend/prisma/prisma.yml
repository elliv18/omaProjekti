hooks:
  post-deploy:
    - graphql get-schema -p prisma

endpoint: http://prisma:3060
datamodel: datamodel.prisma

generate:
  - generator: typescript-client
    output: ../dist/src/generated/prisma-client/