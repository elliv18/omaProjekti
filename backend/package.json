{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start:dev": "yarn prisma delete -f && yarn prisma reset -f && yarn prisma deploy --force && node src/index.js"
  },
  "dependencies": {
    "graphql-cli": "^3.0.14",
    "graphql-yoga": "^1.18.3",
    "prisma-binding": "^2.3.16",
    "prisma": "^1.34.10",
    "prisma-client-lib": "^1.34.10"
  }
}